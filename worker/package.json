{
  "name": "emaildash-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=browser --outfile=dist/index.js",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "postal-mime": "^2.4.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3",
    "wrangler": "^4.12.0"
  }
}
